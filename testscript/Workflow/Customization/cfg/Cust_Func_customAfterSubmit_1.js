

if (screenKey == "Departmentdeleteinstance" && (actionName == "Submit_Workflow")) {
		//alert("request is submitted to server");
		uploadData("getCurrentScreen="+getCurrentScreen());
		end();
	}
	






