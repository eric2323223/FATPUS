	
if (screen === "AllDTdeleteinstance" && menuItem === "Submit_Workflow") {
		 showConfirmDialog("Are you sure you want to delete it?");
		 uploadData("getCurrentScreent="+getCurrentScreen());
		 end();
	    }
	
   return true;







